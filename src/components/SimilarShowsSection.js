import React from 'react'

const SimilarShowsSection = ({ data }) => {
    console.log(data)
  return <div className="similar-cards">
    {
      data.map((item)=>{
        
      })
    }
  </div>;
};

export default SimilarShowsSection
